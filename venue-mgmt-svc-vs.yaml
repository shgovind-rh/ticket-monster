apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: venue-mgmt-svc-vs
  namespace: ticket-monster
spec:
  http:
    - route:
        - destination:
            host: venue-mgmt-svc
